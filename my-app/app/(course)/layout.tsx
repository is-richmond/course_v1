/**
 * Layout for course pages (without sidebar).
 * Course pages get a clean, fullscreen experience without the global navigation sidebar.
 */
export default function CourseLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
