# Полная реализация авторизации - MediCourse Frontend

## 📋 Обзор

Реализована полная система авторизации на фронте с интеграцией с backend auth-сервисом.

## 🏗️ Архитектура

### Backend Endpoints (Python FastAPI)

**Auth Service** (`/auth` prefix):
- `POST /auth/register` - Регистрация новых пользователей
- `POST /auth/login` - Вход в систему (email + password)
- `POST /auth/refresh` - Обновление access token через refresh token
- `POST /auth/change-password` - Смена пароля для авторизованного пользователя

**User Service** (`/auth/users`):
- `GET /auth/users/me` - Получить данные текущего пользователя
- `PATCH /auth/users/me` - Обновить данные профиля

**Enrollment Service** (`/auth/enrollment`):
- `GET /auth/enrollment/my-courses` - Получить список зачисленных курсов
- `POST /auth/enrollment/courses/{course_id}` - Записаться на курс
- `DELETE /auth/enrollment/courses/{course_id}` - Отписаться от курса

---

## 🚀 Реализованные компоненты

### 1. **API Configuration** (`src/lib/api.ts`)
```typescript
- apiClient: axios instance с автоматическим добавлением токена в заголовки
- Interceptors для обработки 401 и автоматического обновления токена
- authAPI: методы для auth операций (register, login, changePassword)
- userAPI: методы для работы с профилем (getCurrentUser, updateProfile)
- enrollmentAPI: методы для управления курсами (getEnrolledCourses, enrollCourse, unenrollCourse)
```

### 2. **Auth Context** (`src/contexts/AuthContext.tsx`)
```typescript
- Глобальное управление состоянием авторизации
- Автоматическая инициализация из localStorage
- Методы: login, register, logout, changePassword, updateProfile, refreshUser
- Тип User с полной информацией о пользователе
```

### 3. **страницы авторизации**

#### Login Page (`src/app/auth/login/page.tsx`)
- Email + Password форма
- Обработка ошибок с красивым отображением
- Ссылка на регистрацию
- Перенаправление на главную после входа
- Loading состояние кнопки

#### Register Page (`src/app/auth/register/page.tsx`)
- Полная форма регистрации:
  - Имя и фамилия (в два поля)
  - Email
  - Номер телефона (с валидацией)
  - Пароль (минимум 8 символов)
  - Подтверждение пароля
- Валидация на клиенте перед отправкой
- Обработка ошибок от backend
- Ссылка на вход для существующих пользователей

#### Change Password Page (`src/app/auth/change-password/page.tsx`)
- Требует авторизации
- Форма смены пароля:
  - Текущий пароль
  - Новый пароль
  - Подтверждение нового пароля
- Валидация:
  - Пароли должны совпадать
  - Минимум 8 символов
  - Новый пароль должен отличаться от старого
- Success/Error сообщения
- Перенаправление на профиль после успешного изменения

### 4. **Страница профиля** (`app/profile/page.tsx`)
- Отображение информации пользователя:
  - Инициалы в аватаре
  - Полное имя и статус
  - Email, телефон, дата регистрации
  - Количество зачисленных курсов
- Редактирование профиля:
  - Имя, фамилия, номер телефона
  - Save/Cancel кнопки
  - Loading state и error handling
- Быстрые ссылки:
  - Смена пароля
  - Мои курсы

### 5. **Обновленные компоненты**

#### Header (`src/components/layout/Header.tsx`)
- Динамическое отображение информации пользователя
- Dropdown меню с опциями:
  - Профиль
  - Смена пароля
  - Выход
- Loading состояние при инициализации

#### Sidebar (`src/components/layout/Sidebar.tsx`)
- Условное отображение меню в зависимости от auth статуса
- Для авторизованных пользователей:
  - Мои курсы
  - Тесты
  - Профиль (с подменю)
  - Кнопка "Выход"
- Для не авторизованных:
  - Кнопка "Вход"
  - Кнопка "Регистрация"

#### My Courses (`app/my-courses/page.tsx`)
- Загрузка курсов из backend API
- Fallback на localStorage если API недоступен
- Динамическое отображение списка курсов

### 6. **UI Компоненты**
- `src/components/ui/Input.tsx` - Input field
- `src/components/ui/Label.tsx` - Label component
- `src/components/ui/Card.tsx` - Card с CardTitle и CardDescription

---

## 🔐 Управление токенами

### Хранение
```
localStorage:
- access_token: JWT токен для API запросов
- refresh_token: Токен для обновления access token
- current_user: Сохраненные данные пользователя (JSON)
```

### Автоматическое обновление
- При получении 401 ответа автоматически:
  1. Использует refresh_token для получения новых токенов
  2. Сохраняет новые токены в localStorage
  3. Повторяет оригинальный запрос
  4. При ошибке refresh - перенаправляет на /auth/login

---

## 📱 Flow диаграммы

### Регистрация
```
Register Page
    ↓
Форма заполнена и валидна
    ↓
authAPI.register()
    ↓
Backend создает пользователя и возвращает tokens
    ↓
Сохраняем tokens и user данные в localStorage
    ↓
Перенаправляем на главную (/)
```

### Вход
```
Login Page
    ↓
Email + Password заполнены
    ↓
authAPI.login()
    ↓
Backend проверяет credentials и возвращает tokens
    ↓
Сохраняем tokens и user данные в localStorage
    ↓
Перенаправляем на главную (/)
```

### API запрос с авторизацией
```
API запрос (например, GET /me)
    ↓
axios interceptor добавляет Authorization header с access_token
    ↓
Backend обрабатывает запрос
    ↓
Если 401 - автоматически обновляем token и повторяем
```

---

## 🎯 Маршруты

| Маршрут | Требует Auth | Описание |
|---------|-------------|---------|
| `/auth/login` | ❌ | Страница входа |
| `/auth/register` | ❌ | Страница регистрации |
| `/auth/change-password` | ✅ | Смена пароля |
| `/profile` | ✅ | Профиль пользователя |
| `/my-courses` | ✅ | Мои курсы |
| `/tests` | ✅ | Список тестов |
| `/certificates` | ✅ | Мои сертификаты |

---

## 🔌 Переменные окружения

```env
# .env.local
NEXT_PUBLIC_API_URL=http://localhost:8000/auth
```

---

## 📦 Установленные пакеты

```json
{
  "axios": "^1.x" - HTTP клиент для API запросов
}
```

---

## 🧪 Тестирование

### Локальная разработка
```bash
# Terminal 1: Start auth backend
cd auth
uvicorn src.app.main:app --reload --port 8000

# Terminal 2: Start frontend dev server
cd my-app
npm run dev
```

Доступно на: `http://localhost:3000`

### Тестовый сценарий
1. Нажать "Регистрация"
2. Заполнить форму регистрации
3. Должно перенаправить на главную с авторизованным пользователем
4. Проверить Header - должно показывать имя и иконку профиля
5. Нажать на профиль в Header - открыть dropdown меню
6. Нажать "Выход" - перенаправить на login

---

## 📋 Типы

### User Interface
```typescript
interface User {
  id: string;
  email: string;
  first_name?: string;
  last_name?: string;
  phone_number?: string;
  is_active: boolean;
  is_superuser: boolean;
  is_verified: boolean;
  created_at: string;
  updated_at?: string;
  enrolled_courses: string[];
}
```

### Auth Context
```typescript
interface AuthContextType {
  user: User | null;
  isLoading: boolean;
  isAuthenticated: boolean;
  login: (email: string, password: string) => Promise<void>;
  register: (data: RegisterData) => Promise<void>;
  logout: () => void;
  changePassword: (oldPassword: string, newPassword: string) => Promise<void>;
  updateProfile: (data: UpdateProfileData) => Promise<void>;
  refreshUser: () => Promise<void>;
}
```

---

## ✅ Завершённые задачи

- ✅ Создан API конфиг с axios и interceptors
- ✅ Реализован Auth Context с управлением состоянием
- ✅ Создана страница входа (login)
- ✅ Создана страница регистрации (register)
- ✅ Создана страница смены пароля (change-password)
- ✅ Обновлена страница профиля с функциональностью
- ✅ Обновлены Header и Sidebar для отображения auth статуса
- ✅ Интегрирована работа с курсами через API
- ✅ Реализована автоматическая сохранение и восстановление сессии
- ✅ Добавлена обработка ошибок и валидация
- ✅ Проект успешно собирается (npm run build)

---

## 🚀 Следующие шаги (опционально)

1. Email верификация при регистрации
2. Восстановление пароля (forgot-password flow)
3. Two-factor authentication (2FA)
4. Социальная авторизация (Google, GitHub)
5. Logout со всех устройств
6. Session management и history

---

## 📞 API документация

API документация доступна на:
- Backend: `http://localhost:8000/auth/docs` (Swagger)
- Backend: `http://localhost:8000/auth/redoc` (ReDoc)

